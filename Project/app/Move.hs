module Move (
    -- Move (..),
    Move,
    parseMove,
    showMove,
)
where

type Move = (Int, Int)

showMove :: Move -> String
showMove = undefined

parseMove :: String -> Move
parseMove = undefined